---
name: Mapper
route: /mapper
---

import {SchemaForm, decorators} from '../src';
import {Playground, Props} from 'docz';

# SchemaForm

This is an example:

<Props of={SchemaForm} />

<SchemaForm form={['*']} schema={{type: 'string'}} decorator={decorators.mui} model="Hello, friend" />
